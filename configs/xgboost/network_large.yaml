# XGBOOST - Network Data - Large Config
# Auto-generated by generate_configs.py

data:
  dataset_type: network
  train_ratio: 0.7
  val_ratio: 0.15
  test_ratio: 0.15
  balancing: smote
  normalize: true
  seed: 42
  nrows_per_file: 800000
model:
  name: xgboost
  hyperparameters:
    n_estimators: 500
    max_depth: 12
    learning_rate: 0.05
    subsample: 0.9
    reg_lambda: 1.0
training:
  epochs: 200
  batch_size: 16
  learning_rate: 0.0005
  optimizer: adamw
  weight_decay: 0.0001
  scheduler: cosine
  early_stopping: true
  patience: 3
  use_gpu: true
experiment:
  name: network_large
  save_predictions: true
  save_history: true
  save_model: true
