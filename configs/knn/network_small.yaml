# KNN - Network Data - Small Config
# Auto-generated by generate_configs.py

data:
  dataset_type: network
  train_ratio: 0.7
  val_ratio: 0.15
  test_ratio: 0.15
  balancing: none
  normalize: true
  seed: 42
  nrows_per_file: 100000
model:
  name: knn
  hyperparameters:
    n_neighbors: 3
    weights: uniform
training:
  epochs: 20
  batch_size: 64
  learning_rate: 0.002
  optimizer: adamw
  weight_decay: 0.0001
  scheduler: cosine
  early_stopping: true
  patience: 5
  use_gpu: true
experiment:
  name: network_small
  save_predictions: true
  save_history: true
  save_model: true
