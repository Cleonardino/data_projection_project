# FT_TRANSFORMER - Network Data - Medium Config
# Auto-generated by generate_configs.py

data:
  dataset_type: network
  train_ratio: 0.7
  val_ratio: 0.15
  test_ratio: 0.15
  balancing: oversampling_copy
  normalize: true
  seed: 42
  nrows_per_file: 5000
model:
  name: ft_transformer
  hyperparameters:
    d_token: 96
    n_heads: 8
    n_layers: 3
    dropout: 0.15
training:
  epochs: 50
  batch_size: 32
  learning_rate: 0.001
  optimizer: adamw
  weight_decay: 0.0001
  scheduler: cosine
  early_stopping: true
  patience: 10
  use_gpu: true
experiment:
  name: network_medium
  save_predictions: true
  save_history: true
  save_model: true
