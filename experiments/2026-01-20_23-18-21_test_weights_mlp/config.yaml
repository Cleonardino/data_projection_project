data:
  balancing: class_weights
  dataset_type: network
  n_samples: 20000
  normalize: true
  nrows_per_file: 5000
  test_ratio: 0.15
  train_ratio: 0.7
  val_ratio: 0.15
experiment:
  name: test_weights
model:
  hyperparameters:
    hidden_layers:
    - 64
    - 32
  name: mlp
training:
  batch_size: 32
  epochs: 2
